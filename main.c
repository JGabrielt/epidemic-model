#include "processor.h"
 
int main(void) {
    Dados* d;
    SIR(d);
    /* double* parametros = entradaParametros();
    for(int i = 0; i < 12; i++)
    {
        printf("%lf\n", parametros[i]);
    } */
 
    return 0;
}